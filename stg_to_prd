> #!/bin/bash
>
> STG_BRANCH = "stg"
> PRD_BRANCH = "prd"
> TIMESTAMP = $(date)
> TAG_NAME = "prd_$TIMESTAMP"
>
> echo "=== Gthtyjc bpvtytybq bp $STG_BRANCH в $PRD_BRANCH ==="
>
> git checkout $STG_BRANCH
> git pull origin $STG_BRANCH
>
> git checkout $PRD_BRANCH
> git pull origin $PRD_BRANCH
>
> git merge $STG_BRANCH --no-ff -m "Merge $STG_BRANCH -> $PRD_BRANCH $TIMESTAM"
>
> EOF






